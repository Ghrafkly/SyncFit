package com.example.syncfit.ui.screens

object ScreenConstants {
    object Route {
        const val START = "start"
        const val SETTINGS = "settings_view"

        object SignIn {
            const val SIGN_IN = "sign_in"
            const val JOIN = "join"
        }

        object Timers {
            const val HOME = "timers_view"
            const val CREATE = "timer_create"
            const val DETAILS = "timer_details"
            const val RUN = "timer_run"
        }

        object Profile {
            const val HOME = "profile_view"
            const val EDIT = "profile_edit"
        }
    }
}
